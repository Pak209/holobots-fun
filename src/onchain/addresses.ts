import addrs from '../../onchain/out/addresses.json';

export const addresses = addrs as {
  baseSepolia: { Holobots721A: `0x${string}`; Parts1155: `0x${string}` };
  base: { Holobots721A: `0x${string}`; Parts1155: `0x${string}` };
};
